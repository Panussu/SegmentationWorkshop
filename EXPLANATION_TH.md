# อธิบายโค้ดและการทำงานทีละส่วน

## 1. ภาพรวม

ภาพสี → หาสีพื้นหลังจากขอบ → คำนวณคะแนนความต่างสี → เทียบ threshold → ได้ mask → ทำ Morphology → เทียบ Ground truth → สร้างผลประเมิน

**Mask** คือภาพที่แต่ละพิกเซลบอกว่าเป็นวัตถุหรือไม่: `True` หรือ 1 หมายถึงสัตว์ (Positive Class), `False` หรือ 0 หมายถึงพื้นหลัง (Negative Class) ส่วน Ground truth คือเฉลยจากชุดข้อมูลที่ใช้ตรวจคำตอบของโปรแกรม

โปรแกรมไม่ได้รู้จักรูปร่างแมวหรือสุนัข แต่ใช้สมมติฐานว่าสัตว์มีสีต่างจากพื้นหลัง จึงต้องอธิบายข้อจำกัดนี้ตอนนำเสนอ

## 2. segmentation.py: ส่วนที่ควรเข้าใจก่อน

### foreground_score(rgb)

รับภาพ RGB ขนาดสูง × กว้าง × 3 และคืนคะแนนขนาดสูง × กว้าง

1. `cv2.cvtColor` แปลง RGB เป็น Lab ซึ่งแยกความสว่างกับองค์ประกอบสี OpenCV ใช้การเข้ารหัส Lab แบบ 8 บิตในขั้นตอนนี้ จึงเป็นระยะห่างเชิงตัวเลขแบบง่าย ไม่ใช่สูตรมาตรฐาน Delta E
2. `astype(np.float32)` เปลี่ยนเป็นเลขทศนิยมก่อนลบค่า เพื่อไม่ให้เลข 8 บิตวนกลับเมื่อผลติดลบ
3. `np.concatenate` รวมพิกเซลขอบบน ล่าง ซ้าย และขวาเป็นรายการเดียว
4. `np.median(..., axis=0)` หาค่ากลางแต่ละช่องสี ได้สีตัวแทนพื้นหลังเพียงหนึ่งสี
5. `np.linalg.norm(..., axis=2)` คำนวณระยะห่างสีของทุกพิกเซลจากสีพื้นหลัง โดยหารากที่สองของผลรวมกำลังสองของผลต่างทั้งสามช่อง
6. `distance / (distance + 50.0)` ปรับคะแนนให้อยู่ระหว่าง 0 ถึงต่ำกว่า 1 สีเหมือนพื้นหลังได้ 0 ส่วนสีที่ต่างมากได้คะแนนสูง เลข 50 เป็นค่าคงที่ในการปรับสเกล ไม่ได้เรียนรู้จาก test และคะแนนนี้ไม่ใช่ความน่าจะเป็นที่ยืนยันความแม่นยำแล้ว

ฟังก์ชันนี้รับเฉพาะภาพ ไม่รับ Ground truth ดังนั้นไม่ได้ใช้เฉลยช่วยสร้างคำตอบ

### clean_mask(mask)

รับ mask และคืน mask ที่ผ่าน Morphology

- `getStructuringElement` สร้าง kernel วงรี 3×3 ซึ่งเป็นบริเวณเล็กที่เลื่อนตรวจรอบพิกเซล
- **Opening** คือ Erosion แล้ว Dilation: ลดจุดวัตถุเล็ก ๆ ที่อาจเป็น noise
- **Closing** คือ Dilation แล้ว Erosion: เติมช่องว่างเล็ก ๆ และเชื่อมบริเวณที่อยู่ใกล้กัน
- แปลงข้อมูลเป็น `uint8` ก่อนส่งให้ OpenCV แล้วคืนเป็น Boolean เพื่อใช้คำนวณผลต่อ

ทั้งสองขั้นตอนใช้ครั้งละหนึ่งรอบ ค่า kernel คงที่ตลอดการทดลอง จุดเล็กที่ลบอาจเป็นรายละเอียดจริง เช่น ขนหรือปลายหู จึงห้ามสรุปว่า Morphology ทำให้ดีขึ้นเสมอ

### confusion_counts(truth, prediction)

เปรียบเทียบ Boolean สองชุดทีละพิกเซล เครื่องหมาย `~` หมายถึง NOT และ `&` หมายถึง AND ส่วน `np.sum` นับจำนวนค่า True

| ค่า | ความหมาย |
|---|---|
| TP | เป็นสัตว์จริง และทำนายเป็นสัตว์ |
| TN | เป็นพื้นหลังจริง และทำนายเป็นพื้นหลัง |
| FP | เป็นพื้นหลังจริง แต่ทำนายเป็นสัตว์ |
| FN | เป็นสัตว์จริง แต่ทำนายเป็นพื้นหลัง |

### metrics(counts)

รับจำนวนทั้งสี่ค่า แล้วคำนวณ:

- Accuracy = (TP + TN) / จำนวนพิกเซลทั้งหมด
- Precision = TP / (TP + FP): พิกเซลที่บอกว่าเป็นสัตว์ มีสัตว์จริงมากแค่ไหน
- Recall = TP / (TP + FN): เก็บพิกเซลสัตว์จริงได้มากแค่ไหน
- IoU = TP / (TP + FP + FN): ส่วนทับซ้อนหารด้วยส่วนรวมของ mask กับเฉลย
- Dice = 2TP / (2TP + FP + FN): วัดความซ้อนทับอีกแบบหนึ่ง

หากตัวหารเป็นศูนย์ โปรแกรมกำหนดค่าเป็น 0 เพื่อไม่ให้หยุดทำงาน ใช้ IoU และ Dice ประกอบ Accuracy เพราะพื้นหลังจำนวนมากอาจทำให้ Accuracy สูงทั้งที่แยกสัตว์ได้ไม่ดี

## 3. download_data.py: เตรียมข้อมูล

`ROOT` คือโฟลเดอร์ที่มีสคริปต์ และ `DATA` คือโฟลเดอร์เก็บข้อมูล จึงเรียกสคริปต์จากที่อื่นได้โดยตำแหน่งข้อมูลไม่เปลี่ยน

`download(name)` ดาวน์โหลด archive เมื่อยังไม่มีไฟล์ เก็บระหว่างดาวน์โหลดด้วยนามสกุล `.part` และเปลี่ยนชื่อหลังสำเร็จ หากการดาวน์โหลดถูกตัดกลางทาง ให้รันคำสั่งเดิมอีกครั้ง โปรแกรมจะเริ่มดาวน์โหลดไฟล์นั้นใหม่

`main()` ทำตามลำดับ:

1. ดาวน์โหลด `annotations.tar.gz` ซึ่งมีรายชื่อภาพและ trimap
2. ใช้ `random.Random(42)` เพื่อให้สุ่มรายชื่อเดิมเมื่อทำซ้ำ เลือก validation 20 ภาพจากรายการ trainval ของต้นทาง และ test 50 ภาพจากรายการ test ของต้นทาง
3. เก็บ trimap ของภาพที่เลือกใน `data/masks/` โดยอ่านเฉพาะสมาชิก archive ที่ต้องการ
4. ดาวน์โหลด `images.tar.gz` และเก็บภาพที่เลือกใน `data/images/`
5. ตรวจว่าภาพครบ แล้วบันทึกรายชื่อแต่ละชุดใน `data/split.json`

เราไม่ฝึกโมเดล จึงไม่ต้องมี training set เพิ่ม แต่ยังต้องมี validation เพื่อเลือก threshold โดยไม่ดูคะแนน test ก่อน

## 4. run.py: เชื่อมทุกขั้นตอน

### load_sample(name)

อ่านภาพด้วย Pillow แล้วแปลงเป็น RGB อ่าน mask โดยไม่แปลงเป็น RGB เพื่อรักษาค่า label 1, 2, 3 ตรวจว่าขนาดตรงกันและ label ถูกต้อง

ย่อภาพโดยรักษาสัดส่วนให้ด้านยาวไม่เกิน 256 พิกเซลเพื่อลดเวลา ใช้ `INTER_AREA` สำหรับภาพสี และ `INTER_NEAREST` สำหรับ trimap เพราะไม่ควรผสมตัวเลข label ระหว่างการย่อ

คืนค่า 3 อย่าง: ภาพ RGB, เฉลยสัตว์ (`trimap == 1`), และตำแหน่งที่ประเมินได้ (`trimap != 3`) พิกเซลขอบไม่แน่นอนจึงไม่ถูกบังคับให้เป็นสัตว์หรือพื้นหลังในการให้คะแนน

### choose_threshold(names)

คำนวณ score ของ validation 20 ภาพ แล้วลอง threshold ตั้งแต่ 0.10 ถึง 0.90 เพิ่มทีละ 0.05 เมื่อ `score >= threshold` ให้เป็นสัตว์ จากนั้นเลือก threshold ที่ให้ค่าเฉลี่ย IoU รายภาพสูงสุดก่อน Morphology หากเสมอกันจะเลือกตัวแรกซึ่งมี threshold ต่ำกว่า

เก็บผลทุก threshold เพื่อให้ตรวจสอบได้ การเลือกใช้ mean IoU รายภาพช่วยให้ภาพใหญ่ไม่กลบภาพเล็กในขั้นตอนนี้ ส่วนผลสุดท้ายรายงานแบบรวมพิกเซล ซึ่งต้องแยกความหมายให้ชัด

### save_example(...)

สร้างภาพห้าช่อง: ภาพจริง, Ground truth, mask ก่อน Morphology, mask หลัง Morphology และภาพตัดพื้นหลัง สีเทาใน Ground truth คือบริเวณที่ไม่ประเมิน ส่วน cutout ใช้สีดำแทนพื้นหลัง ไม่ได้สร้างความโปร่งใส

### main()

1. อ่าน split และตรวจว่าชุดข้อมูลไม่ทับกันและมี test อย่างน้อย 50 ภาพ
2. เลือก threshold จาก validation ก่อน
3. สำหรับ test แต่ละภาพ คำนวณคะแนน → threshold → Morphology โดยใช้ค่าที่ล็อกไว้แล้ว
4. ใช้ `truth[valid]` และ `prediction[valid]` ตัดพิกเซล label 3 ออกจากการนับ
5. เก็บ TP, TN, FP, FN และ metrics รายภาพ พร้อมสะสมจำนวนสำหรับผลรวม
6. เก็บคะแนนต่อเนื่องและเฉลยของพิกเซล test เพื่อคำนวณ ROC/AUC
7. บันทึก JSON, CSV, ตาราง Markdown, Confusion Matrix และภาพตัวอย่างครบ 50 ภาพ

คำสั่ง `if __name__ == "__main__"` ทำให้ `main()` ทำงานเมื่อเรียกไฟล์โดยตรง แต่ไม่รันทันทีเมื่ออีกไฟล์ import ฟังก์ชันไปใช้

## 5. อ่าน ROC Curve อย่างไร

ROC เปลี่ยน threshold ของคะแนนแล้ววาดแกน X = FPR = FP/(FP+TN) และแกน Y = TPR = Recall ยิ่งโค้งเข้าใกล้มุมซ้ายบนยิ่งดี AUC เป็นพื้นที่ใต้กราฟ ค่าใกล้ 1 หมายถึงจัดอันดับพิกเซลได้ดี ส่วนประมาณ 0.5 คือการจัดอันดับใกล้การสุ่ม

โค้ดส่ง **score ต่อเนื่อง** เข้า `roc_curve` และ `roc_auc_score` ไม่ส่ง mask 0/1 เพราะ mask มีข้อมูลเพียงผลของ threshold เดียว จุด Before และ After บนกราฟเป็นผลที่ threshold ที่เลือก โดย After ผ่าน Morphology เพิ่มแล้ว ไม่มีการอ้างว่าเส้น ROC เป็นผลหลัง Morphology

ROC/AUC นี้รวมพิกเซลที่ประเมินได้จาก test ทั้งหมด ภาพที่มีจำนวนพิกเซลมากจึงมีน้ำหนักมากกว่า และไม่ใช่ AUC สำหรับจำแนกว่าทั้งภาพเป็นแมวหรือสุนัข

## 6. การตรวจสอบ

`test_segmentation.py` ตรวจจำนวน Confusion Matrix จากคำตอบที่รู้แน่นอน, ตรวจว่าสีวัตถุที่ต่างจากพื้นหลังได้คะแนนสูงกว่า และตรวจว่า Opening ลบจุดโดดเดี่ยวโดยยังเก็บบริเวณวัตถุใหญ่ไว้ การทดสอบเหล่านี้ตรวจความถูกต้องของกลไก ไม่ได้ยืนยันว่าแยกสัตว์จริงได้แม่นยำ

การรัน `run.py` บน test 50 ภาพคือการวัดประสิทธิภาพจริง ให้ดู `outputs/RESULTS.md` และภาพตัวอย่างประกอบ ห้ามใช้ภาพตัวอย่างจำลองใน unit test อ้างเป็นผลทดลองชุดข้อมูลจริง

## 7. แนวทางนำเสนอแบบสั้น

1. โจทย์: แยกแมวและสุนัขเป็น Positive และพื้นหลังเป็น Negative
2. ข้อมูล: Oxford-IIIT Pet, validation 20 ภาพ, test 50 ภาพ, Ground truth และ label ที่ไม่นำมาคิดคะแนน
3. อัลกอริทึม: อธิบาย `foreground_score` และการเลือก threshold
4. Morphology: อธิบาย Opening/Closing แล้วเปิดภาพก่อนและหลังทั้งตัวอย่างที่ดีและที่ผิดพลาด
5. ผล: แสดง Confusion Matrix, IoU/Dice และ ROC/AUC ที่ได้จริง
6. อภิปราย: เปรียบเทียบก่อน/หลัง หากคะแนนลดลงให้อธิบายว่ารายละเอียดใดถูกลบหรือเชื่อมผิด ไม่เปลี่ยนค่าเพื่อทำให้ test ดูดี
7. ข้อจำกัด: พื้นหลังหลายสี สัตว์สีคล้ายพื้นหลัง สัตว์ติดขอบภาพ และรายละเอียดที่หายจากการย่อภาพ

สมาชิกกลุ่มสามารถแบ่งหน้าที่เป็นเตรียมข้อมูล, อธิบาย Segmentation, อธิบาย Morphology, ประเมินผล และรวบรวมการนำเสนอ โดยทุกคนควรเข้าใจลำดับการทำงานทั้งหมด

## 8. อภิปรายผลการรันที่แนบมา

การรันนี้เลือก threshold 0.40 จาก validation 20 ภาพ แล้วประเมิน test 50 ภาพ รวมพิกเซลที่ใช้ประเมิน 2,139,052 พิกเซล ได้ ROC AUC 0.7440 ของคะแนนความต่างสี

| Metric | ก่อน Morphology | หลัง Morphology |
|---|---:|---:|
| Accuracy | 0.6225 | 0.6414 |
| Precision | 0.4622 | 0.4779 |
| Recall | 0.7893 | 0.7828 |
| IoU | 0.4114 | 0.4219 |
| Dice | 0.5830 | 0.5935 |

Morphology ลด FP จาก 656,811 เป็น 611,628 พิกเซล แต่เพิ่ม FN จาก 150,671 เป็น 155,347 พิกเซล หมายความว่าเอาพื้นหลังที่ติดมาออกได้บางส่วน แต่เสียพิกเซลสัตว์จริงไปด้วย ผลรวม IoU และ Dice ดีขึ้นเล็กน้อย ขณะที่ Recall ลดลง

ภาพ `outputs/examples/Abyssinian_25.png` แสดงข้อจำกัดชัดเจน: mask ยังเก็บส่วนของฉากหลัง และตัวแมวบางส่วนหายไป แม้ Morphology ลดจุดเล็กได้ แต่แก้การแยกสีผิดเป็นบริเวณใหญ่ไม่ได้ คะแนนนี้จึงเป็นผลของวิธีพื้นฐานบนชุดย่อยที่เลือก ไม่ใช่ข้อสรุปว่าวิธีนี้แยกสัตว์ได้ดีในทุกภาพ

หากรันใหม่ด้วยโค้ดหรือข้อมูลที่เปลี่ยนไป ให้ยึด `outputs/RESULTS.md` และ `outputs/summary.json` ที่สร้างใหม่เป็นหลัก และปรับคำอภิปรายให้ตรงกับผลใหม่
