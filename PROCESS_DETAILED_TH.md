# คำอธิบายกระบวนการ Segmentation Workshop แบบละเอียด

เอกสารนี้อธิบายการทำงานของโปรเจกต์ตั้งแต่เริ่มเตรียมข้อมูลจนถึงการอ่านผลลัพธ์ โดยยึดตามโค้ดในไฟล์ `.py` จริง โปรเจกต์ใช้วิธีพื้นฐานที่เหมาะสำหรับเรียนรู้หลักการ Image Segmentation: พิกเซลที่มีสีแตกต่างจากพื้นหลังมีแนวโน้มเป็นสัตว์

## 1. เป้าหมายของโปรเจกต์

โจทย์คือแยกแมวหรือสุนัขออกจากพื้นหลัง ภาพสัตว์ถูกกำหนดเป็น **Positive Class** และพื้นหลังเป็น **Negative Class** โปรแกรมทำงานระดับพิกเซล ไม่ได้จำแนกว่าภาพทั้งภาพเป็นแมวหรือสุนัข

วัตถุประสงค์หลักมี 4 ข้อ:

1. เตรียมภาพอย่างน้อย 50 ภาพพร้อม Ground truth
2. ออกแบบวิธีสร้าง mask จากภาพจริง
3. ใช้ Morphology ปรับปรุง mask
4. วัดผลด้วย Confusion Matrix, Accuracy, Precision, Recall, IoU, Dice และ ROC/AUC

วิธีนี้เป็น baseline สำหรับการเรียนรู้ ไม่ใช่โมเดล Deep Learning จึงอาศัยสมมติฐานว่าบริเวณขอบภาพส่วนใหญ่เป็นพื้นหลัง

## 2. โครงสร้างไฟล์

| ไฟล์หรือโฟลเดอร์ | หน้าที่ |
|---|---|
| `download_data.py` | ดาวน์โหลด archive และเลือกภาพกับ trimap |
| `segmentation.py` | ฟังก์ชันคำนวณคะแนนสี Morphology และ metrics |
| `run.py` | ควบคุมการทดลองทั้งหมดและสร้างผลลัพธ์ |
| `test_segmentation.py` | ทดสอบฟังก์ชันหลักด้วยข้อมูลขนาดเล็กที่รู้คำตอบ |
| `json_comments.py` | เขียน JSON พร้อมคำอธิบายใน `_comment` |
| `data/` | archive ภาพ mask และ `split.json` |
| `outputs/` | ตาราง JSON CSV กราฟและภาพตัวอย่าง |

## 3. แหล่งข้อมูลและ Ground truth

ใช้ [Oxford-IIIT Pet Dataset](https://www.robots.ox.ac.uk/~vgg/data/pets/) ซึ่งมีภาพแมวและสุนัข พร้อม pixel-level trimap สำหรับทุกภาพ ชุดข้อมูลต้นทางมี label 3 ค่า:

| Label | ความหมายในต้นฉบับ | การใช้ในโค้ด |
|---:|---|---|
| 1 | Foreground | แปลงเป็น `truth = True` หรือสัตว์ |
| 2 | Background | แปลงเป็น `truth = False` หรือพื้นหลัง |
| 3 | Boundary ที่ไม่แน่นอน | ไม่ใช้ประเมิน และแสดงเป็นสีเทา |

การไม่คิด label 3 ป้องกันไม่ให้โปรแกรมถูกลงโทษจากขอบภาพที่ Ground truth เองระบุว่าไม่แน่นอน

## 4. ขั้นตอนที่ 1: ดาวน์โหลดและแบ่งข้อมูล

### `download_data.py`

ฟังก์ชัน `download(name)` สร้างพาธ `data/name` แล้วตรวจว่ามีไฟล์อยู่หรือไม่ ถ้ายังไม่มี จะดาวน์โหลดจาก URL ของ Oxford-IIIT Pet ลงไฟล์ชั่วคราว `.part` ก่อน เมื่อดาวน์โหลดสำเร็จจึงเปลี่ยนชื่อเป็นไฟล์จริง วิธีนี้ช่วยไม่ให้ไฟล์ดาวน์โหลดค้างถูกเข้าใจว่าเสร็จแล้ว

ใน `main()` โปรแกรมทำงานดังนี้:

1. สร้างโฟลเดอร์ `data/`
2. ดาวน์โหลด `annotations.tar.gz`
3. เปิดรายการ `trainval.txt` และสุ่ม 20 ชื่อเป็น `validation`
4. เปิดรายการ `test.txt` และสุ่ม 50 ชื่อเป็น `test`
5. ใช้ `random.Random(42)` เพื่อให้การสุ่มทำซ้ำได้ชื่อเดิม
6. ดึงเฉพาะ trimap ของชื่อที่เลือกไปไว้ใน `data/masks/`
7. ดาวน์โหลด `images.tar.gz`
8. อ่าน archive แบบ stream และดึงเฉพาะภาพที่เลือกไปไว้ใน `data/images/`
9. ตรวจว่าภาพที่ต้องการถูกดึงมาครบ
10. บันทึกรายชื่อสองกลุ่มลง `data/split.json`

การแบ่งนี้สำคัญเพราะ validation ใช้ตัดสินใจเลือก threshold ส่วน test ต้องเก็บไว้ประเมินผลหลังจากตัดสินใจแล้ว หากใช้ภาพชุดเดียวกันทั้งสองหน้าที่ ผลอาจดูดีเกินจริง

## 5. ขั้นตอนที่ 2: โหลดและย่อภาพ

ฟังก์ชัน `load_sample(name)` ใน `run.py` เปิดภาพ JPG ด้วย Pillow แล้วแปลงเป็น RGB จากนั้นเปิด PNG trimap โดยรักษาค่า 1, 2 และ 3 ไว้

โปรแกรมตรวจ 2 เงื่อนไขก่อนทำงาน:

- ขนาด trimap ต้องตรงกับความสูงและความกว้างของภาพ
- ค่า label ต้องอยู่ใน `{1, 2, 3}` เท่านั้น

ภาพถูกย่อให้ด้านยาวไม่เกิน 256 พิกเซล โดยไม่ขยายภาพที่เล็กอยู่แล้ว ภาพสีใช้ `INTER_AREA` ซึ่งเหมาะกับการลดขนาด ส่วน trimap ใช้ `INTER_NEAREST` เพื่อไม่ให้ interpolation สร้างค่า label ใหม่ เช่น 1.5

ฟังก์ชันคืนค่า 3 ชุด:

```python
rgb                 # ภาพสี RGB
trimap == 1         # truth: True เฉพาะบริเวณสัตว์
trimap != 3         # valid: True เฉพาะพิกเซลที่นำมาประเมินได้
```

## 6. ขั้นตอนที่ 3: สร้างคะแนนความเป็นวัตถุ

ฟังก์ชัน `foreground_score(rgb)` อยู่ใน `segmentation.py` และไม่รับ Ground truth เข้าไป จึงสร้างคำตอบจากภาพจริงเท่านั้น

### 6.1 แปลง RGB เป็น Lab

`cv2.cvtColor(rgb, cv2.COLOR_RGB2LAB)` เปลี่ยนภาพจาก RGB เป็น Lab ช่อง Lab แยกความสว่างและองค์ประกอบสี ทำให้การวัดความแตกต่างของสีทำได้เป็นระบบกว่าใช้ RGB ตรง ๆ โปรแกรมแปลงเป็น `float32` ก่อนคำนวณ เพื่อให้ผลต่างติดลบได้โดยไม่เกิดการวนค่าของเลข 8 บิต

### 6.2 ประมาณสีพื้นหลังจากขอบภาพ

โปรแกรมรวมพิกเซลจากขอบบน ขอบล่าง ขอบซ้าย และขอบขวาเข้าด้วยกัน แล้วใช้ `np.median(..., axis=0)` หาค่าสีมัธยฐาน 3 ช่อง สีนี้เป็นตัวแทนพื้นหลังหนึ่งสี

สมมติฐานคือขอบภาพส่วนใหญ่เป็นพื้นหลัง หากสัตว์กินพื้นที่ขอบมาก หรือพื้นหลังมีหลายสี สีตัวแทนเพียงสีเดียวอาจไม่เพียงพอ

### 6.3 คำนวณระยะห่างและปรับสเกล

`np.linalg.norm(lab - background, axis=2)` คำนวณระยะห่างแบบ Euclidean ระหว่างสีของแต่ละพิกเซลกับสีพื้นหลัง พิกเซลที่ต่างมากได้ระยะมาก จากนั้นคำนวณ:

```text
score = distance / (distance + 50.0)
```

คะแนนอยู่ระหว่าง 0 ถึงต่ำกว่า 1 ค่า `50.0` เป็นค่าคงที่สำหรับปรับสเกล ไม่ใช่ค่าความน่าจะเป็น และไม่ได้เรียนรู้จาก test

## 7. ขั้นตอนที่ 4: เลือก Threshold

ฟังก์ชัน `choose_threshold(names)` ทำงานกับภาพ validation 20 ภาพเท่านั้น โปรแกรมลองค่า threshold ตั้งแต่ `0.10` ถึง `0.90` เพิ่มทีละ `0.05`

สำหรับแต่ละค่า จะทำ `score >= threshold` เพื่อสร้าง mask ชั่วคราว แล้วคำนวณ IoU ของทุกภาพ จากนั้นหาค่าเฉลี่ย IoU รายภาพ โปรแกรมเลือก threshold ที่มีค่าเฉลี่ยสูงสุด ในการทดลองนี้ได้ `0.40`

ผลการลองทุกค่าอยู่ใน `outputs/threshold_search.json` ตัวอย่างความหมาย:

```json
{
  "threshold": 0.4,
  "mean_iou": 0.4271834310895103
}
```

ค่า threshold ต่ำจะเลือกพิกเซลมากขึ้น จึงอาจเก็บพื้นหลังมาด้วย ค่า threshold สูงจะเลือกเฉพาะพิกเซลที่ต่างจากพื้นหลังมาก แต่อาจทำให้ส่วนของสัตว์หายไป

## 8. ขั้นตอนที่ 5: Morphology

ฟังก์ชัน `clean_mask(mask)` สร้าง kernel รูปวงรี 3×3 แล้วทำ 2 operation:

### Opening

Opening คือ Erosion ตามด้วย Dilation ช่วยลบจุดสีขาวเล็ก ๆ ที่แยกออกจากวัตถุหลัก เหมาะสำหรับลด noise แต่จุดเล็กที่เป็นรายละเอียดจริง เช่น ขนหรือปลายหู อาจถูกลบด้วย

### Closing

Closing คือ Dilation ตามด้วย Erosion ช่วยเติมรูเล็ก ๆ และเชื่อมบริเวณสีขาวที่อยู่ใกล้กัน อาจทำให้ขอบวัตถุเรียบขึ้น แต่ถ้าพื้นหลังอยู่ใกล้กันมาก ก็อาจเชื่อมสิ่งที่ไม่ควรเชื่อม

OpenCV ต้องการข้อมูล `uint8` จึงแปลง Boolean mask เป็น 0/1 ก่อน แล้วแปลงผลกลับเป็น Boolean หลัง Closing

## 9. ขั้นตอนที่ 6: วนทดสอบภาพ 50 ภาพ

`main()` ใน `run.py` อ่าน `data/split.json` และตรวจว่ามี test อย่างน้อย 50 ภาพและไม่มีชื่อซ้ำกับ validation จากนั้นทำขั้นตอนต่อไปกับแต่ละภาพ test:

1. โหลด RGB, truth และ valid
2. คำนวณ `score`
3. ใช้ threshold ที่เลือกจาก validation สร้าง `raw`
4. ส่ง `raw` เข้า `clean_mask()` ได้ `cleaned`
5. เปรียบเทียบทั้ง `raw` และ `cleaned` กับ truth เฉพาะตำแหน่ง `valid`
6. สะสมจำนวน TN, FP, FN และ TP
7. บันทึกผลรายภาพลง `per_image.csv`
8. สร้างภาพ Input, Ground truth, Before, After และ Cutout ลง `outputs/examples/`

การใช้ `truth[valid]` และ `prediction[valid]` หมายถึง label 3 ถูกตัดออกจากทั้งเฉลยและผลทำนายก่อนนับคะแนน

## 10. Confusion Matrix และ metrics

ฟังก์ชัน `confusion_counts(truth, prediction)` ใช้ Boolean operation ของ NumPy เพื่อนับผล:

| ค่า | ความหมาย |
|---|---|
| TP | สัตว์จริงและทำนายเป็นสัตว์ |
| TN | พื้นหลังจริงและทำนายเป็นพื้นหลัง |
| FP | พื้นหลังจริงแต่ทำนายเป็นสัตว์ |
| FN | สัตว์จริงแต่ทำนายเป็นพื้นหลัง |

ฟังก์ชัน `metrics(counts)` คำนวณ:

```text
Accuracy  = (TP + TN) / (TP + TN + FP + FN)
Precision = TP / (TP + FP)
Recall    = TP / (TP + FN)
IoU       = TP / (TP + FP + FN)
Dice      = 2TP / (2TP + FP + FN)
```

Accuracy อาจสูงได้แม้ mask สัตว์ไม่ดี เพราะภาพจำนวนมากเป็นพื้นหลัง ดังนั้นควรดู IoU และ Dice ร่วมกับ Precision และ Recall

## 11. ROC และ AUC

โปรแกรมเก็บ `score` ต่อเนื่องของพิกเซล test แล้วส่งให้ `roc_curve()` และ `roc_auc_score()` ค่า threshold จะเปลี่ยนไปตลอดเส้น ROC:

- แกน X คือ FPR หรือสัดส่วนพื้นหลังที่ถูกทำนายผิดเป็นสัตว์
- แกน Y คือ TPR หรือ Recall
- AUC คือพื้นที่ใต้เส้น ROC ค่าใกล้ 1 หมายถึงการจัดอันดับพิกเซลตามคะแนนทำได้ดี

ROC ใช้คะแนนก่อน threshold เพราะถ้าใช้ mask 0/1 จะมี operating point เพียงจุดเดียว ไม่ใช่เส้นโค้งเต็มรูปแบบ จุด Before และ After ในกราฟเป็นจุดของ threshold เดียว โดย After ผ่าน Morphology แล้ว

## 12. ไฟล์ JSON และความหมายของ `_comment`

JSON มาตรฐานไม่มี comment แบบ `//` ดังนั้นโปรเจกต์เก็บคำอธิบายไว้ใน field ชื่อ `_comment` ซึ่งเป็นข้อมูลปกติที่ parser อ่านได้

### `data/split.json`

เก็บ array ชื่อภาพใน `validation` และ `test` เพื่อให้รันซ้ำชุดเดิมได้

### `outputs/split.json`

เป็นสำเนา split ที่ใช้สร้างผลลัพธ์ครั้งนั้น เพื่อเป็นหลักฐานตรวจสอบย้อนกลับ

### `outputs/threshold_search.json`

เป็นรายการค่าที่ลองทั้งหมด แต่ละ object มี threshold และ mean IoU ของ validation

### `outputs/summary.json`

เก็บจำนวนภาพ ขนาดภาพ จำนวนพิกเซล AUC และผลรวม Confusion Matrix พร้อม metrics ก่อนและหลัง Morphology

การใส่ `_comment` ไม่เปลี่ยนข้อมูลจริง โปรแกรม `json.loads()` จะอ่าน field นี้ได้ตามปกติ และโค้ดเลือกใช้เฉพาะ key ที่ต้องการ เช่น `split["test"]`

## 13. ผลการทดลองที่มีอยู่

ผลจากการรัน test 50 ภาพและพิกเซลที่ประเมิน 2,139,052 พิกเซล:

| Metric | ก่อน Morphology | หลัง Morphology |
|---|---:|---:|
| Accuracy | 0.6225 | 0.6414 |
| Precision | 0.4622 | 0.4779 |
| Recall | 0.7893 | 0.7828 |
| IoU | 0.4114 | 0.4219 |
| Dice | 0.5830 | 0.5935 |

Morphology ลด FP จาก 656,811 เหลือ 611,628 แต่เพิ่ม FN จาก 150,671 เป็น 155,347 จึงลดพื้นหลังที่ติดมาได้บางส่วนพร้อมกับเสียพิกเซลสัตว์บางส่วน ผลรวม IoU และ Dice เพิ่มขึ้นเล็กน้อย ส่วน Recall ลดลงเล็กน้อย

ROC AUC ของคะแนนสีต่อเนื่องเท่ากับ `0.7440` ค่านี้บอกความสามารถในการจัดอันดับพิกเซลตามความแตกต่างของสี ไม่ใช่ความแม่นยำของการจำแนกภาพทั้งภาพ

## 14. ข้อจำกัด

วิธีนี้อาจผิดพลาดในกรณีต่อไปนี้:

- สัตว์มีสีใกล้กับพื้นหลัง
- พื้นหลังมีหลายสีและ median สีเดียวอธิบายไม่ได้
- สัตว์สัมผัสขอบภาพ ทำให้สีพื้นหลังที่ประมาณผิด
- พื้นหลังต่างจากขอบภาพ แต่โปรแกรมเลือกเป็นวัตถุ
- Opening ลบรายละเอียดเล็กของสัตว์
- Closing เชื่อมพื้นหลังเข้ากับวัตถุ
- การย่อภาพทำให้รายละเอียดบางส่วนหายไป

ดังนั้นผลการทดลองควรใช้เพื่ออธิบายหลักการและเปรียบเทียบพฤติกรรมของ algorithm ไม่ควรอ้างว่าเป็นระบบ segmentation ที่พร้อมใช้งานจริง

## 15. วิธีรันและตรวจสอบ

ใน PowerShell เปิดโฟลเดอร์โปรเจกต์แล้วใช้:

```powershell
.venv/Scripts/python.exe download_data.py
.venv/Scripts/python.exe run.py
.venv/Scripts/python.exe -m unittest -v
```

ถ้ามีข้อมูลอยู่แล้ว สามารถรันเฉพาะ `run.py` ได้ ผลลัพธ์ใหม่จะเขียนทับ JSON ตาราง และกราฟใน `outputs/` โดยใช้ split เดิม

Unit tests ตรวจ 3 เรื่อง: การนับ Confusion Matrix จากคำตอบที่รู้แน่นอน, คะแนนของวัตถุที่มีสีต่างจากพื้นหลัง และการที่ Opening ลบจุดโดดเดี่ยวแต่ยังเก็บวัตถุใหญ่ไว้ Unit tests ตรวจกลไกของโค้ด ไม่ได้แทนการประเมินภาพจริง 50 ภาพ

## 16. สรุปผังงานแบบข้อความ

```text
เริ่ม
  ↓
ดาวน์โหลด Oxford-IIIT Pet และ trimap
  ↓
สุ่ม validation 20 ภาพ / test 50 ภาพด้วย seed 42
  ↓
โหลดภาพและย่อด้านยาวไม่เกิน 256 พิกเซล
  ↓
แปลง RGB → Lab
  ↓
หาสี median จากขอบภาพเป็นพื้นหลัง
  ↓
คำนวณระยะห่างสีของทุกพิกเซล → score 0..1
  ↓
ลอง threshold หลายค่าใน validation
  ↓
เลือก threshold ที่ mean IoU สูงสุด
  ↓
นำ threshold ไปใช้กับ test 50 ภาพ
  ↓
สร้าง raw mask
  ↓
Opening → Closing ได้ cleaned mask
  ↓
ตัด label 3 ออกจากการประเมิน
  ↓
นับ TN / FP / FN / TP
  ↓
คำนวณ metrics และ ROC/AUC
  ↓
บันทึก JSON / CSV / PNG / Markdown
  ↓
จบ
```

ผังงานนี้แสดงเหตุผลของการแยก validation และ test รวมถึงตำแหน่งที่ Ground truth ถูกใช้: ใช้เลือก threshold และตรวจผล แต่ไม่ถูกส่งเข้า `foreground_score()` เพื่อช่วยสร้าง mask
