# =============================================================================
# ส่วนที่ 1: ไลบรารีสำหรับคำนวณและประมวลผลภาพ
# =============================================================================
# ติดตั้ง NumPy สำหรับจัดการอาร์เรย์ภาพและคำนวณเชิงตัวเลข
numpy==2.5.3
# ติดตั้ง OpenCV แบบไม่ใช้หน้าต่างสำหรับแปลงสี ย่อภาพ และทำ Morphology
opencv-python-headless==5.0.0.93

# =============================================================================
# ส่วนที่ 2: ไลบรารีสำหรับประเมินผลและสร้างกราฟ
# =============================================================================
# ติดตั้ง scikit-learn สำหรับคำนวณ ROC curve และ ROC AUC
scikit-learn==1.9.0
# ติดตั้ง Matplotlib สำหรับสร้างภาพผลลัพธ์และกราฟประเมิน
matplotlib==3.11.1

# =============================================================================
# ส่วนที่ 3: ไลบรารีสำหรับอ่านและแปลงไฟล์ภาพ
# =============================================================================
# ติดตั้ง Pillow สำหรับอ่านไฟล์ภาพ RGB และ Trimap
pillow==12.3.0
